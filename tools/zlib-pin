# The zlib source that tools/build-llvm.cmake compiles into the Linux LLVM
# tools. lld reads the debug sections of the musl objects of Alpine, which
# carry SHF_COMPRESSED on x86_64, so it needs zlib to decompress them. The
# version stands in ZLIB_VERSION alone, and @VERSION@ below takes it.
ZLIB_VERSION=1.3.1
url=https://zlib.net/fossils/zlib-@VERSION@.tar.gz
digest=9a93b2b7dfdac77ceba5a558a580e74667dd6fede4585b91eefb60f03b72df23
