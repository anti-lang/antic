# The pinned raylib release that tools/get-raylib.cmake downloads. GitHub
# publishes no digest for a source archive, so the digest below is the one
# of the archive that antic was first tested with.
RAYLIB_VERSION=6.0
RAYLIB_DIGEST=2b3ee1e2120c7a0796b33062c7e9a694dd8a8caa56a96319ac8c8ecf54a90d0b
# The miniaudio version that the release bundles in src/external/miniaudio.h.
# raylib takes the ma_ functions from the miniaudio library of the runtime
# tree, so tools/miniaudio-pin names the same version, which the test
# raylib_miniaudio_pin checks.
RAYLIB_MINIAUDIO_VERSION=0.11.24
