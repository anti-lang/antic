# The pinned Mbed TLS release that src/native/get-mbedtls.cmake downloads,
# of the 3.6 LTS line. The digest is the SHA-256 of
# mbedtls-<version>.tar.bz2 that the release page of Mbed-TLS/mbedtls
# publishes in mbedtls-<version>-sha256sum.txt.
MBEDTLS_VERSION=3.6.7
MBEDTLS_DIGEST=a7e8bcbec0e6f761b4af24f25677626b35f762f68eef79c08677a363212d11f6
