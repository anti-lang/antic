//! A shift by the width of the type. Each target reads a different part
//! of the count, so the value without the checks is not compared.

fn opaque(n: int) -> int
{
	return n;
}

fn sink(n: int)
{
}

fn main() -> int
{
	sink(opaque(1) << opaque(64));
	return 7;
}
