spillweight.hot:
b0:
    pushq %rbp
    movq %rsp, %rbp
    subq $112, %rsp
    movq %rbx, 104(%rsp)
    movq %r12, 96(%rsp)
    movq %r13, 88(%rsp)
    movq %r14, 80(%rsp)
    movq %r15, 72(%rsp)
    movq %rdi, %r10
    movq %r10, 56(%rsp)
    movq 56(%rsp), %r10
    addq $1, %r10
    movq %r10, 56(%rsp)
    movq %rdi, %rsi
    addq $2, %rsi
    movq %rdi, %rdx
    addq $3, %rdx
    movq %rdi, %rcx
    addq $4, %rcx
    movq %rdi, %r8
    addq $5, %r8
    movq %rdi, %r9
    addq $6, %r9
    movq %rdi, %rbx
    addq $7, %rbx
    movq %rdi, %r12
    addq $8, %r12
    movq %rdi, %r13
    addq $9, %r13
    movq %rdi, %r14
    addq $10, %r14
    movq %rdi, %r15
    addq $11, %r15
    movq $0, %r10
    movq %r10, 48(%rsp)
    movq $0, %rax
b1:
    cmpq %rdi, %rax
    jge b3
b2:
    movq 48(%rsp), %r10
    addq %rax, %r10
    movq %r10, 48(%rsp)
    addq $1, %rax
    jmp b1
b3:
    movq %rbx, (%rsp)
    movq %r12, 8(%rsp)
    movq %r13, 16(%rsp)
    movq %r14, 24(%rsp)
    movq %r15, 32(%rsp)
    movq %rax, 40(%rsp)
    movq 56(%rsp), %r10
    movq %r10, %rdi
    call use12
    movq 48(%rsp), %r10
    movq %r10, %rcx
    addq %rax, %rcx
    movq %rcx, %rax
    movq 104(%rsp), %rbx
    movq 96(%rsp), %r12
    movq 88(%rsp), %r13
    movq 80(%rsp), %r14
    movq 72(%rsp), %r15
    movq %rbp, %rsp
    popq %rbp
    ret
