union Value
{
	i: int,
	f: f64
}

struct Flags
{
	visible: u32 : 1,
	layer: u32 : 4,
	level: u8 : 9
}

struct A align(3)
{
	a: u8
}

fn f(flags: Flags) -> int
{
	let v = Value { i: 1, f: 2.0 };
	let p = &flags.layer;
	return 0;
}
