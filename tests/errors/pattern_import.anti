//! A pattern literal, `Regex.compile` and the methods of `str` with a pattern
//! belong to `anti.regex`, which this module does not import, and `Regex` names
//! one function.

fn main() -> int
{
	let literal = re"a+";
	let compiled = Regex.compile("a+") catch fatal;
	let other = Regex.parse("a+");
	return 0;
}

fn search(r: Regex, s: str)
{
	let m = s.matches(r) catch none;
}
