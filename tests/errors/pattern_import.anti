//! A pattern literal and `Regex.compile` belong to `anti.regex`, which this
//! module does not import, and `Regex` names one function.

fn main() -> int
{
	let literal = re"a+";
	let compiled = Regex.compile("a+") catch fatal;
	let other = Regex.parse("a+");
	return 0;
}
