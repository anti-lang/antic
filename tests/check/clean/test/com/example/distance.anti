//! A program of the test directory, which the check reads as well.

import com.example.geometry;

fn main() -> int
{
	let p = geometry.Point { x: 1, y: 1 };
	if geometry.taxicab(p) != 2 { return 1; }
	return 0;
}
